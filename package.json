{
  "name": "croma-auth",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch bd.json --port 3000"
  },
  "devDependencies": {
    "json-server": "0.17.4"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
